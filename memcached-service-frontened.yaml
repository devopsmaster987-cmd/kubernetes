apiVersion: v1
kind: Service
metadata:
  labels:
    app: mc01
  name: vprocache01
spec:
  ports:
    - name: memchached
      port: 11211
      protocol: TCP
      targetPort: memcached
  selector:
    app: vpromc
  type: ClusterIP

